name: Deploy dev

on:
  workflow_run:
    workflows: ["Run Build"]
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    if: github.event.workflow_run.conclusion == 'success' && startsWith(github.event.workflow_run.head_branch, 'feature/')
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to dev
        run: echo "Deploy to dev"  # (仮)デプロイコマンド
